# Existing Grok routine input alignment R01

Status: OWNER_AUTHORIZED_ADDITIVE_UPDATE_PREPARED; SAVED_PROVIDER_CONFIGURATION_UNAVAILABLE.
Approval and scope: [R01 maintenance intent](https://github.com/opus99999/pulsechain-mcp/issues/43#issuecomment-5553445792). This public addendum contains no existing private Bot prompt and does not replace one.

Recipient: existing Head Chef Of Workers, provider agent 098a040d-daa5-4c65-973c-e903d892d324, PC Ground Zero group 66374919-0516-416c-a21f-4962491aa16b.
Modify only existing routine `grok-x-protocol-hourly-monitor`, in place. Do not create a routine or bot. Keep its reported enabled=true and `CRON_TZ=UTC 12 * * * *`; preserve every valid existing legacy monitoring responsibility. Defer :45. Other observer routines, legacy pulsechain-signal-watch and Infrastructure collector safeguards are unchanged.

## One provider-side update

1. Resolve this exact existing routine through your supported provider management function. Read current inputs, enabled state and schedule; preserve the existing text as a protected rollback reference. Do not export that private text. If another update already installed an equivalent R01 addendum, read it back and suppress a duplicate update.

2. Read [the deployed protocol](https://pulsechain-research-control-room.brohexphiat.chatgpt.site/operations/grok-operations-v1.md), [current operations queue](https://pulsechain-research-control-room.brohexphiat.chatgpt.site/api/v1/grok-operations) and [continuity checkpoint](https://pulsechain-research-control-room.brohexphiat.chatgpt.site/api/v1/continuity). The protocol's R01 revision is identified by UTF-8 SHA-256 `48f6ec170c3ba64275a018679dc960661c191c4040ba4522ebb274271b70c4d9`. A missing or differing revision requires read-only reconciliation before operations writes.

3. Through your own protected supported Direct Web context, GET `/api/v1/grok-operations/session`. The server maps website `grok-x-protocol` to your exact provider ID. For this account the expected capability literals are `GROK_MASTER_CHEF_OPERATIONS` and `GROK_PROVISIONAL_OBSERVER`. The first is the coordinator event role verbatim; there is no mapping from `GROK_MASTER_OPERATIONS`. If the shorter or another unexpected string is still returned, make no production POST and do not try role alternatives. Return only the response URL, UTC time, HTTP status, schema version, website/provider identity, allowed_acting_roles, write_readiness and csrf_present boolean. Never return the cookie, CSRF value, authentication headers, session export or private prompts. Do not request new enrollment based only on the old report. Foreground success does not establish routine-session access.

4. Append the following bounded behavior to the routine's existing valid monitoring instructions; do not replace the whole prompt:

- On each normal :12 opportunity, independently read `GET /api/v1/grok-operations` and every currently configured legacy Inquisitor hitter/master-chef/question queue. Keep each queue's read status, cutoff and addressed-item count separate. Empty operations requests do not mean legacy queues are empty; legacy inactivity does not prove an operations read. Unavailable is not empty.
- Preserve healthy legacy monitoring and its existing evidence boundaries. A browser-dependent source blocks only that source. Operations work must not run when its contract/session checks fail. If the provider cannot isolate incompatible writes, pause only this exact routine through supported management and read back the pause; do not pause the other observers or claim a pause without evidence.
- Initially allow only the already-owner-authorized single `SYNTHETIC_TRANSPORT_CANARY` for the R01/federation vertical slice. Head Chef must first create and accept its exact canonical opening and record that request ID in issue43/current shared continuation. No canary ID has been created by this addendum. Bind to that exact accepted ID; if it is missing, multiple, expired, terminal, conflicting or lacks authority, do no operations write. Reject unrestricted live requests during initial validation. Never create the opening yourself or manufacture a result for an empty queue.
- Verify the opening receipt `pulsechain-grok-operations-receipt@1.0.0`, accepted=true, exact event/hash/request, synthetic=true, purpose=SYNTHETIC_TRANSPORT_CANARY and question_id=null. Copy its exact target worker/workstream, addressed observer, evidence cutoff, expiry and request binding. Read `/api/v1/grok-operations/requests/{source_request_id}` and check existing results/receipts before acting.
- Use the confirmed literal role for each permitted step. Coordinator `GROK_MASTER_CHEF_OPERATIONS`: GROK_TEAM_EVENT PICKED_UP, ASSIGNED and CLOSED only when the stored phase permits; provider_agent_id is your existing ID and website_identity_when_applicable=null. OPERATIONS_STATUS is coordinator-only and is not the provisional packet required by this canary. Never use coordinator authority for an observer finding.
- Send the exact bounded synthetic work to the opening's existing addressed observer through SendToAgent, or perform it as your distinct observer role only if the opening targets grok-x-protocol. Preserve one team ID, existing fixed identities, request team limit <=5 and Master Chef pivot. Current V1 validator limits the event team to the Master Chef and the opening's addressed observer; do not add helpers beyond this implemented scope.
- The addressed observer alone returns one GROK_OPERATIONS_RESULT with result_kind=PROVISIONAL_EVIDENCE_PACKET, acting_role=GROK_PROVISIONAL_OBSERVER, its own mapped provider ID and exact website identity. You never originate another observer's event. ChatGPT's responsible specialist alone supplies GROK_SPECIALIST_FEEDBACK. Close only after its exact accepted feedback; no specialist publication, pointer move, research conclusion, Head Chef lifecycle or Worker #5 event.
- Construct canonical events using the published schema/hash specification and existing canonical builder where available. Use only your own protected trusted session, scoped `x-grok-operations-csrf` and exact existing POST route. No authentication material leaves your protected context; no GitHub connector or credential change is needed.
- After each permitted append, require accepted=true with exact event/request/content hash, receipt ID and D1 sequence, then read the exact stored request and receipt before advancing. An ambiguous write requires reconciliation of that same immutable event, never a replacement. Exact replay is a no-op; conflicting content stops. Stop at an actor transfer, a genuine gate or terminal state. End the run rather than waiting.
- Require zero research/publication/pointer/financial effects and zero manual result relay for the canary. Record only actual exposed provider run IDs and timings. Keep configured inputs, foreground reads, scheduled queue pickup, scheduled-session access, accepted write and specialist feedback as separate evidence. Do not call federation active until the complete path is observed.

5. Use one supported in-place provider update only after inspecting the exact current saved routine. Preserve enabled=true and UTC :12 if operations writes can remain isolated; do not introduce a pause/resume cycle merely for this update. Read back the saved routine. If the update outcome is uncertain, reconcile its exact provider-returned identifier and current saved inputs before retry; no replacement routine.

## Required nonsecret completion readback

Return one bounded result: provider/group IDs; exact routine ID/name; inspection and saved-update UTC times; enabled and schedule before/after; public R01 addendum hash; provider revision/update ID if exposed; has_grok_operations=true as independently read back; separate legacy-input preservation status; exact allowed_acting_roles and readiness classification from the protected GET without values; operations-write gate state; duplicate/uncertain-write check; actual foreground versus scheduled evidence; mutation outcome or exact blocker. Do not include the full existing saved prompt.

If no authorized operations request exists, report NO_ADDRESSED_OPERATIONS_REQUEST with zero production writes. Do not invent a status event merely to report setup. Return this one setup result in the existing provider context; subsequent authorized canary packets and feedback use the website bridge without owner relay. ChatGPT will not claim this update installed or create the canary before its actual saved readback.

