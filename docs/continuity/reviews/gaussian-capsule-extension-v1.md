# Gaussian capsule Work Hub extension — verified release v1.0.0

Recorded: 2026-09-07T19:38:30Z (September 7, 2026, 3:38:30 p.m. EDT). Maintainer: incumbent ChatGPT Head Chef, acting as the existing Control Center implementation function. Classification: owner-authorized foreground implementation; private inputs; public metadata-only result.

**Deployed and privately staged; Claude launch remains disabled. `NO_INVOCATION_ATTEMPTED`.** This record advances the [original capsule design](../modules/control-room-evidence-job-capsule-v1.md) and [prepared connection at 88742d9](https://github.com/opus99999/pulsechain-mcp/blob/88742d9809cb7229335c28ce6050636de39c6394/operations/claude-capsule-v1/WORK_HUB_COMPATIBILITY.md), under the owner's September 7 bounded extension authority. The [pre-mutation operation intent](https://github.com/opus99999/pulsechain-mcp/issues/43#issuecomment-5574734676) retains the prior configuration and recovery point. It is not another architecture proposal, provider result, or Gaussian capability claim.

## Exact source and release

| Component | Verified identity |
|---|---|
| Protected application source | `fba9768fcd3ff9342a805a9d98249e4c5fe57016`, pushed and saved as Sites version 55 |
| Saved source archive | 3,880,960 bytes, 93 files; SHA-256 `35a370395032f8e9b5fdc15faa47313cd8aa056f20d24c2867d5d39ec48e5e02` |
| Deployment | `appgdep_6a9f11236db08191881a1585e3fc59fd`, environment revision 48; native status `succeeded`, 2026-09-07T19:32:20.973360Z (3:32:20 p.m. EDT) |
| Production destination | Existing [Control Room](https://pulsechain-research-control-room.brohexphiat.chatgpt.site); audience and unrelated environment entries unchanged |
| Private execution repository | `opus99999/pulsechain-research-control-room-bridge`; repository ID `1345447956`, owner ID `212180323`, private; identity reverified |
| Published workflow source | [Commit f00f8b9](https://github.com/opus99999/pulsechain-research-control-room-bridge/commit/f00f8b9fde4aafe4ac32894f4e1898542e4741de); exact `.github/workflows/gaussian-l1-integrity.yml` blob `e7e3343aca4e8ee7fdae59bbd5dfee567a403a6a`; published/read back before server binding |
| Additive migration | `0020_single_capsule_review.sql`, 1,719 bytes; SHA-256 `2d87de3e18839ae7961f3bf21e19311245eb886500e3032a4d4d859c7133b624` |
| Recovery source | Version 54, source `2de29303298c7fe12b3ab9c80cb5b34630e891a4`, prior environment revision 47 |

The private workflow has an unconditional false job condition. The server is separately in **STAGING**, which rejects every reviewer operation. No workflow dispatch, runtime installation, canary, model call, or benchmark execution was performed. Publishing the workflow does not prove execution; changing it requires reviewing/publishing the new bytes and binding that actual immutable commit before activation.

## Bounded contract implemented

Capsule: `gaussian-l1-integrity-v0.1.0-20260907`. Separate route prefix: `/api/v2/work-hub/capsule/gaussian-l1-integrity-v0.1.0-20260907/`.

- One new append-only D1 table, `work_hub_capsule_events`, and capsule-specific private R2 keys. No specialist/canary table, authorization or finalization contract was replaced. No general provider registry or service was added.
- GitHub RS256 signature/JWKS verification plus server-side exact issuer, capsule audience, immutable subject, repository/owner IDs, private visibility, branch, actor, event, workflow/source SHA, workflow ref, hosted-runner, run/check-run, first-attempt, time and JTI checks. Request-body role claims and repository names alone confer no scope. Reusable-workflow claims are not accepted by this binding.
- One atomic producer run/check-run reservation, fresh single-use OIDC authorization per request, 64-request reviewer ceiling and 20-minute lease. Tokens are not stored. GitHub and Work Hub credentials stay in trusted orchestration, outside the Claude/model tool process.
- Exactly the two input IDs **and hashes** below, plus their immutable metadata manifest. Neither a same-named replacement nor an arbitrary artifact ID is readable.
- One private result reservation binds producer, input manifest, output contract, result ID `integrity-result`, named consumer and 256 KiB limit. Its future content hash is null because the bytes do not yet exist; there is no wildcard completion hash.
- Result upload must be a complete canonical JSON bundle carrying the prepared JSON/Markdown review, detached member checksums and producer receipt metadata. Actual stored bytes are hashed before immutable commitment. Conflicts, incomplete uploads and checksum mismatches reject; exact repeat finalization returns the original receipt without another final event.
- Only the named Head Chef control can retrieve the complete committed result. Reviewers cannot retrieve its body after production; public and ordinary-worker credentials cannot read evaluator/result bytes. No raw result, evaluator answer, transcript, secret or token is written into public artifacts or logs.

The Head Chef control is authenticated by an explicitly configured capsule-specific signing capability, not a name supplied in a request. Its signed requests bind method, exact URL, capsule/manifest, body hash, short validity and a single-use nonce. Live authorized status and full input retrieval passed. **Limitation:** the platform does not expose independent signed conversation-ID attestation; capability possession authenticates this control, not cryptographic per-chat isolation. Future successor custody is unverified. Local file permissions are not proof of isolation between contexts sharing the same account workspace. No succession or transfer is authorized. Lost or inconsistent custody requires stopping this control and authenticated administration of an exact replacement public key; it does not authorize reconstruction or disclosure of a private key.

## Actual protected staging and readback

All objects were reconciled as absent before creation, then uploaded once through the new authorized route. Full subsequent download matched exact byte count and SHA-256; native D1 readback independently matched all three `INPUT_VERIFIED` receipts without truncation.

| Artifact ID | Bytes | SHA-256 | Stored receipt time (UTC) |
|---|---:|---|---|
| `gaussian-l1-v010-candidate` | 5,931 | `b07e0a124a748a8c3cd3c7e308816a2b8aacbe033968d8aa296d78d9103cdf4e` | `2026-09-07T19:33:50.058Z` |
| `gaussian-l1-v010-evaluator` | 4,615,537 | `ac71b2f9e3a2528a91b681bbf0048f5b57c5ba062583e599f9e4eba30ba8658f` | `2026-09-07T19:33:53.731Z` |
| `capsule-manifest` | 3,960 | `00076c3fa8271b05e09b613741003ade49e76878428e4ec3e0b3161efba95c81` | `2026-09-07T19:33:55.758Z` |

Receipt identities are respectively `input:gaussian-l1-v010-candidate`, `input:gaussian-l1-v010-evaluator` and `input:capsule-manifest`, each within the exact capsule. Original ZIPs, cutoffs, source references and historical checksum exceptions remain unchanged. The old manifest bytes were not rewritten to manufacture current IDs; this contract/receipts provide the verified mapping.

At post-staging readback there were 3 input receipts and 8 Head Chef authorization-use records; **0 reviewer run reservations, 0 result uploads, 0 result commitments, 0 result-consumption records**. This is actual private input staging, not Claude retrieval. A live unauthenticated evaluator request returned HTTP 401 and no artifact bytes. Complete result retrieval is implemented and fixture-tested, but cannot have live result evidence before a real result exists.

## Tests, preservation and recovery

**110 passed, 0 failed, 0 skipped:** 30 capsule tests and 80 existing regressions. Positive tests used the exact original ZIP/manifest bytes in isolated SQLite/R2, then a clearly synthetic software-only review fixture to test reservation, result commitment, lost-response reconciliation and full Head Chef retrieval. The fixture is not an observation, research question, Claude invocation or model capability result.

Rejection coverage: wrong issuer/audience/subject/repository/owner/source/workflow/event/actor; wrong run/check-run/retry attempt; forged, expired, future or replayed authorization; wrong consumer; other capsule/artifact/arbitrary scope; specialist/canary authority reuse; public/ordinary-reader access; truncated/mismatched input; premature finalization; conflicting result/checksum; corrupted stored bytes; request-budget exhaustion. Legitimate scope passes while these cases reject.

All prior SQL schema objects and seeded rows through migration 0019 compared equal after the additive migration. The full 337-statement schema passed both whole-script and split-statement application, integrity check, and zero foreign-key violations. Existing specialist/canary/Grok/Head Chef/runtime suites, typecheck, application build, whitespace checks and control-script syntax passed. One pre-existing historical catalog test expected the migration-0017 table count while reading later migrations; its fixture was pinned to 0017, with current-migration preservation tests retained. No runtime validation was weakened. Native production table presence/rows were verified separately; native full DDL comparison is not exposed.

Rollback: set only `WORK_HUB_CAPSULE_MODE=DISABLED` and deploy that configuration. Offline checks prove denial without deletion, and re-entering STAGING preserves rows. If source rollback is necessary, version 54 is compatible with retained additive table/R2 data. Never reverse migration 0020, erase audit history, clear accepted objects, or reset an attempt to simulate no commit. A lost upload/finalization reply requires exact stored readback before any permitted retry; an ambiguous invocation is `ATTEMPTED_OUTCOME_UNKNOWN`, not `NO_INVOCATION_ATTEMPTED`.

## One private execution-readiness handoff — launch still held

**Owner action:** approve the pinned temporary runtime bootstrap below if an existing suitable binary cannot be verified; privately confirm that the existing Claude account includes the requested Opus 5 model and sufficient included usage, with paid extra usage off, and that the private GitHub job fits included Actions capacity with charged overage prevented. Then reuse an already-authorized subscription OAuth secret, or save one directly through the existing private repository's Actions-secret settings after the handling path is ready. Do not paste any token into chat. Unknown secret/account fields do not mean absent.

The minimum proposed runtime is official native **Claude Code 2.1.259, Linux x64**, downloaded to a fresh ephemeral `ubuntu-24.04` job directory only after approval. [Official manifest](https://downloads.claude.ai/claude-code-releases/2.1.259/manifest.json): expected binary 216,677,784 bytes, SHA-256 `f7dd62ae415378018cd21dd950eb3bac174ab085830304d3b8b098146bfd47b6`. Its metadata was read; **binary download/install/execution and manifest-signature verification were not performed**. Before accepting bootstrap, verify the detached manifest signature against the official pinned primary fingerprint `31DDDE24DDFAB679F42D7BD2BAA929FF1A7ECACE`, then the exact binary bytes/hash and version. The [native setup documentation](https://code.claude.com/docs/en/setup) controls dependencies and signature procedure.

Use existing runner Python 3, gpg, CA trust and native/glibc dependencies; stop on any missing dependency rather than implicitly installing packages. No npm/Bun/global installation, persistent runner, new container image or hosting is approved here. Disable automatic updates, use no floating version alias, and dispose of the ephemeral workspace without uploading caches, private inputs or transcripts. Temporary binary storage is approximately 207 MiB; the job retains a 20-minute ceiling, 600-second review ceiling, 12 turns, 8 MiB input limit and 256 KiB result limit. No additional charge is authorized.

Exact runner isolation remains unverified: a no-secret fixture must show that outside-workspace access, inherited GitHub/Work Hub credentials, arbitrary egress and child execution are denied while the narrow input interface and required Anthropic transport work. CLI tool restrictions alone and [Anthropic's Bash sandbox](https://code.claude.com/docs/en/sandboxing) do not prove whole-process isolation. No packaged validation code is executed by the reviewer in this first job. If the existing runner cannot meet the isolation test, identify that concrete capability gap and keep launch held; do not install an isolation system implicitly.

Once those controls are ready, the existing signed-in Claude Code account can use `claude setup-token` and store/reuse `CLAUDE_CODE_OAUTH_TOKEN` directly in the private repository's [Actions secrets settings](https://github.com/opus99999/pulsechain-research-control-room-bridge/settings/secrets/actions). That OAuth authorization is account-scoped, not a provider-issued capsule-only credential. It goes only to the trusted pinned job's Claude process, never to Work Hub tools, logs or artifacts. No broad Claude GitHub App, API-key billing, plan change or purchase is required or performed by this extension. Subscription authentication and [GitHub Actions usage](https://docs.github.com/en/billing/concepts/product-billing/github-actions) are separate readiness checks; [official Claude Actions documentation](https://code.claude.com/docs/en/github-actions) describes the subscription route.

The final wired workflow must be published, reviewed and rebound to its actual new immutable SHA before activation. It must inspect actual structured provider session/model fields for the requested `claude-opus-5`, stop on an unapproved substitution, and keep provider metadata distinct from Work Hub storage receipts. No token is requested before a workable private handling route. The original at-most-one ordinary live capsule allowance remains unused. No owner-carried artifact relay occurred during staging; this is not yet an end-to-end zero-relay execution result.

## Independent work and evidence boundaries

PHIAT issue 44 was freshly reconciled: last accepted sequence remains 28, receipt [5561363607](https://github.com/opus99999/pulsechain-mcp/issues/44#issuecomment-5561363607). Signals / `chatgpt-worker-1`, `signals-platform`, retains the next action: originate the corrected `SPECIALIST_PUBLICATION_ACCEPTED` event preserving sequence-28 decision, dependencies and summary exactly. Head Chef originated no replacement and did not retry the exhausted publisher repair. The current Head Chef queue exposed no eligible owner-answer transition.

Grok posting, keys, feed review, schedules, conversations, specialist authority and accepted findings were not changed. Gaussian preparation remains independent: 3 selected scene IDs, 18 source-backed questions, 14 unfilled challenge slots, no scene/view assets or model execution claimed by this release. No framework, renderer, world-model system or research repository was installed. Existing source-review lessons remain: shared Grok computer means shared trust boundary; concise references aid selective retrieval; discovery, invocation, authorization and accepted results are separate capabilities.

Invocation state for this pass: **NO_INVOCATION_ATTEMPTED**. Provider receipt, actual Claude model/usage, reviewer output, Head Chef substantive result disposition and outcome verification remain **unavailable because no review was launched**.
